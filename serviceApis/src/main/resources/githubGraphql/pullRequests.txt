query getPullRequests {
  repository(owner: "${repoOwnerName}", name: "${repoName}") {
    pullRequests(${stateParam}headRefName: "${headRef}", first: 5) {
        totalCount,
        nodes ${pullRequestResponse}
     }
  }
}
